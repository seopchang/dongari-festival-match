# 진행 상황

마지막 갱신: **2026-08-25**

---

## 지금 어디까지 됐나

### ✅ 끝난 것

**저장소 · 배포**
- GitHub 저장소 생성: https://github.com/seopchang/dongari-festival-match (public)
- GitHub Pages 배포 완료 — `main` 브랜치 `/ (root)`
  - 참가자용: https://seopchang.github.io/dongari-festival-match/
  - 운영진용: https://seopchang.github.io/dongari-festival-match/admin/
  - `main`에 푸시하면 1~2분 뒤 자동 반영된다

**앱 구현**
- 화면 10개 전부 (시작 → 기본정보 → 질문 4개 → 한마디 → 분석 → 결과/대기)
- 축마다 문항 3개 중 1개 랜덤 출제, localStorage 저장 → 새로고침해도 같은 문항
- 제출 전 뒤로가기 허용 / 제출 후 브라우저 뒤로가기까지 차단
- 인스타 ID 중복 참여 확인 (화면2 + 제출 직전, 2중)
- 인스타 ID 복사 버튼 (clipboard API 폴백 포함)
- 운영진 대시보드: 로그인, 참가자·매칭 표, MBTI 분포, CSV 내려받기

**Firebase**
- 프로젝트: `duthegee-eaf1b` (표시 이름은 `dongari-festival-match`)
- Cloud Firestore `(default)` 생성 완료
- 보안 규칙 배포 완료
- Authentication 이메일/비밀번호 사용 설정 완료
- 운영진 계정 생성 완료
- 승인된 도메인에 `seopchang.github.io` 추가 완료

**검증 완료 (Firestore REST API로 실제 확인)**

| 항목 | 결과 |
| --- | --- |
| API 키 유효성 | ✅ 200 |
| `participants` 읽기 | ✅ 허용됨 |
| `matches` 읽기 (비로그인) | ✅ 403 차단 |
| 정상 참가자 문서 쓰기 | ✅ 통과 |
| 학년 9 (범위 밖) | ✅ 403 차단 |
| 성별 `alien` | ✅ 403 차단 |
| 대문자 인스타 ID | ✅ 403 차단 |
| 임의 필드 `isAdmin` 주입 | ✅ 403 차단 |
| 희망 학년 0개 | ✅ 403 차단 |
| 제출된 응답 수정 | ✅ 403 차단 |
| 제출된 응답 삭제 | ✅ 403 차단 |

---

### ✅ 2026-08-25 작업분

- **"AI가 분석 중" 문구 제거** — AI가 아니라 결정론적 규칙이라 문구를 고침
- **매칭 알고리즘 전면 교체** — MBTI 완전 일치 필수 → **학년 절대 조건 + MBTI 순위**
  - 절대 조건: 성별 반대 + **학년 희망이 양쪽 다 충족**
  - 통과자 중 MBTI 최다 일치자, 동점이면 랜덤
  - 조건 통과자가 없으면 **대기 화면** (희망 학년을 하나만 고른 참가자에게 자주 남)
  - 궁합 %는 **MBTI만** 반영 (`js/config.js`의 `MBTI_PERCENT`, 40~100%)

  설계가 세 번 바뀐 이력 — 다시 논의될 때 참고할 것:
  1. *학년도 감점(−10/방향)* → MBTI가 학년 희망을 덮어쓰는 문제. "3학년만"
     골랐는데 MBTI 잘 맞는 1학년이 뽑힘
  2. *감점을 40으로 올려 학년 우선* → **불가능.** 퍼센트가 음수(−12%)가 되고
     보안 규칙의 `compatibility >= 0`에 걸려 매칭 기록 저장이 조용히 실패한다
  3. *학년 단계별 폴백* → 희망 학년에 사람이 없으면 차선으로 내려감.
     "무조건 매칭"은 되지만 참가자가 고른 조건을 어기게 됨
  4. **현재: 학년을 절대 조건으로.** 운영진 판단 — 조건을 어길 바에는
     대기 화면이 낫다

- **프로필 사진 (선택 항목)** — `js/photo.js`, `photos` 컬렉션
  - 화면 2의 동그라미를 누르면 시트가 올라오고 거기서 고른다
  - 📷 사진 찍기(`capture="user"`) / 앨범 / 동물상 9종 / 안 할래요
  - 브라우저에서 480px 정사각형 JPEG(품질 0.72)로 압축 → 40~70KB.
    한도(800KB)를 넘으면 품질을 자동으로 더 낮춘다
  - EXIF 회전을 `createImageBitmap(..., {imageOrientation:'from-image'})`로
    미리 적용한다. 안 하면 세로 사진이 눕는다
  - **Cloud Storage를 안 쓴 이유**: 버킷이 없다(404). 최근에 만든 Firebase
    프로젝트는 Storage에 Blaze(카드 등록)가 필요해서 Firestore 문자열로 넣었다
  - **`photos`를 별도 컬렉션으로 둔 이유**: 참가자 문서에 넣으면 매칭할 때
    이성 전원의 사진을 내려받는다(100명이면 수 MB). 따로 두면 상대 1장만 받는다

- **동물상 9종** — `js/animals.js`, 전부 인라인 SVG
  - 강아지·고양이·토끼·곰·여우·사슴·공룡·햄스터·늑대 + "안 할래요"(기본 얼굴)
  - 참가자 문서의 `animal` 필드에 id 저장. 안 고르면 필드를 아예 안 넣는다
    (규칙이 문자열만 받아서 null 을 넣으면 저장이 거부된다)
  - 동물을 추가·수정하려면 `animals.js` 만 고치면 된다. 선택 버튼은 목록에서
    자동 생성된다

- **`[hidden]` 버그 2건 수정** — `css/admin.css`, `css/styles.css`
  `.login { display:flex }` 와 `.avatar img { display:block }` 이 HTML의
  `hidden` 속성(`display:none`)을 우선순위에서 이기고 있었다. 각각 로그인 폼이
  안 사라지던 문제와 아바타에 세로줄이 생기던 문제의 원인이었다.
  두 CSS 모두 `[hidden] { display: none !important }` 를 넣어 못 박았다.

> ⚠️ **소스 파일을 PowerShell로 읽고 쓰지 말 것.**
> Windows PowerShell 5.1은 BOM 없는 파일을 CP949로 읽어서 한글 주석을 전부
> 깨뜨린다. 실제로 `admin/admin.js` 가 이렇게 한 번 날아갔다(dist 사본에서 복구).
> 편집은 에디터로 하고, 검사만 PowerShell로 할 것.
- **궁합 퍼센트 표시** — 결과 화면 카드 우상단 배지, 관리자 매칭 표에도 열 추가
- **"다음 사람 차례 · 처음부터 다시" 버튼** — 결과·대기 화면. localStorage를
  비우고 새로고침. 폰 한 대를 여러 명이 돌려 쓰는 부스용
- **관리자 삭제 기능** — 행별 / 매칭만 / 전체 (전체는 `삭제` 타이핑 확인)
- **관리자 로그인 창이 안 사라지던 버그 수정** — `.login`의 `display:flex`가
  `hidden` 속성을 이기고 있었음. `[hidden] { display:none !important }`
- **CSV 다운로드 간헐적 실패 수정** — `revokeObjectURL`을 너무 빨리 부르고 있었음

**대기자 실시간 알림은 안 넣기로 결정.** 부스 현장 일회성 이벤트이고 참가자들이
결과 화면을 캡쳐하는 방식이라 폴링/푸시가 필요 없다. 캡쳐를 놓친 사람은 운영진
대시보드의 매칭 기록에서 찾아주면 된다.

---

## ⚠️ 당장 해야 할 것

### 0. 규칙 배포 — 2026-08-25 기준 완료됨 ✅

REST로 확인한 현재 상태:

| 컬렉션 | 비로그인 읽기 | |
| --- | --- | --- |
| `participants` | 200 허용 | 매칭 계산에 필요 |
| `photos` | 200 허용 | 상대 사진 표시에 필요 |
| `matches` | 403 차단 | 로그인한 운영진만 |

**코드에서 참가자 문서에 새 필드를 추가할 때마다 규칙도 같이 배포해야 한다.**
`isValidParticipant` 의 `hasOnly` 목록에 없는 필드가 하나라도 있으면 참가자
등록이 통째로 거부된다. 지금까지 이걸로 세 번 걸렸다
(`compatibility`, `photos` 컬렉션, `animal`).

### 1. 테스트 문서 삭제 (급함)

규칙 검증하느라 실제 참가자 문서를 하나 만들었다. **아직 안 지웠다.**

- Firebase 콘솔 → Firestore → 데이터 탭
- `participants` → **`ZZ_RULE_TEST_DELETE_ME`** → ⋮ → 문서 삭제

**안 지우면 실제 참가자와 매칭된다.** 매칭이 점수제로 바뀌면서 **더 위험해졌다** —
예전에는 ESTP끼리만 붙었지만, 이제는 MBTI가 하나도 안 맞아도 후보에 들어간다.
여자 참가자가 적은 시간대면 이 가짜 계정이 최고점자가 되어버릴 수 있다.

위 0번(규칙 재배포)을 끝냈다면 **운영진 대시보드에서 행별 삭제 버튼으로 지울 수
있다.** 아직이라면 Firebase 콘솔에서만 가능하다.

### 2. 브라우저 실제 테스트 (한 번도 안 해봄)

**여기가 가장 큰 미검증 영역이다.** Firebase 연결과 규칙은 확인했지만,
화면이 실제로 그려지는지·매칭 로직이 도는지는 브라우저로만 알 수 있다.

폰 1대로도 된다 (다시하기 버튼이 생겼으므로). 인스타 ID는 매번 다르게.

0. **프로필 시트** — 동그라미를 눌러 시트가 올라오는지. 안에서
   📷 사진 찍기 / 앨범 / 동물상 9종 / 안 할래요 가 다 보이는지.
   고르면 시트가 닫히고 동그라미에 반영되는지. 배경·✕·Esc 로 닫히는지.
   ⚠️ **카메라는 폰에서만 열린다.** PC에서는 파일 탐색기가 뜨는 게 정상이다
1. **첫 번째**: 남 / 1학년 / 희망 **2학년** / 답변 **전부 A** / **사진 넣기** → 제출
   → 여자 참가자가 없으므로 **대기 화면**이 떠야 정상
2. 결과 화면 아래 **"다음 사람 차례 · 처음부터 다시"** → 확인 → 시작 화면으로
   → **질문이 새로 뽑혔는지** 확인 (같은 질문이 나오면 버그)
3. **두 번째**: 여 / 2학년 / 희망 **1학년** / 답변 **전부 A** / **동물상 고르기**
   → **매칭 성공** + **궁합 100%** (MBTI 완전 일치)
   → 상대(첫 번째)의 **사진이 동그랗게** 떠야 한다. 이게 사진 저장·조회가
     둘 다 되는지 확인하는 지점이다
4. **세 번째**: 여 / 3학년 / 희망 **3학년** / 답변 **전부 B**
   → 첫 번째는 1학년인데 내 희망은 3학년뿐 → 학년 조건 불충족
   → **대기 화면이 떠야 정상.** 여기서 매칭되면 학년 필터가 안 먹은 것이다
5. **학년 절대 조건 확인 — 이번 변경의 핵심이라 꼭 해볼 것**

   매칭은 제출 시점에 한 번만 도므로, **미끼 두 명을 먼저 넣고** 마지막에
   확인용 참가자를 넣어야 한다.

   | 순서 | 성별/학년 | 희망 학년 | 답변 | MBTI |
   | --- | --- | --- | --- | --- |
   | 미끼 1 | 남 / **2학년** | 3학년 | 전부 B | INFJ |
   | 미끼 2 | 남 / **3학년** | 3학년 | 전부 A | ESTP |
   | 확인용 | 여 / **3학년** | 3학년 | 전부 B | INFJ |

   확인용 참가자에게는 **미끼 2(ESTP)** 가 떠야 정상이다. 궁합은 **40%**.

   - 미끼 1은 MBTI가 완벽히 같지만(INFJ), 2학년이라 **확인용의 희망(3학년)에
     안 걸린다** → 후보에서 탈락
   - 미끼 2는 MBTI가 정반대지만 서로 3학년을 원해 조건을 통과한다
   - **미끼 1(궁합 100%)이 뜨면 학년 필터가 안 먹은 것이다**
6. **관리자**: `/admin/` 로그인 →
   - 로그인 폼이 **사라지는지** (이번에 고친 버그)
   - 참가자 표의 **사진 열** — 사진 넣은 사람은 썸네일, 동물상 고른 사람은
     그 그림, 아무것도 안 한 사람은 기본 얼굴
   - 썸네일 클릭 → 새 탭에서 크게 열리는지
   - 매칭 기록 탭의 **A 사진 / B 사진 / 궁합** 열
   - 행별 **삭제 버튼**이 실제로 동작하는지 (참가자를 지우면 사진도 같이 사라짐)
   - **매칭 기록만 초기화** → 참가자는 남아 있는지
7. 끝나면 **전체 초기화**로 정리

막히면 **F12 콘솔의 빨간 오류를 그대로** 복사해둘 것.

### 로컬로 띄우는 법

이 PC에는 Node도 Python도 없어서(설치된 `python.exe`는 Store 스텁) PowerShell
전용 정적 서버 `serve.ps1`을 만들어 뒀다.

```powershell
powershell -ExecutionPolicy Bypass -File serve.ps1
```

→ 참가자용 http://localhost:8000/ · 운영진용 http://localhost:8000/admin/

`file://`로 열면 ES 모듈이 CORS로 막혀 하얀 화면이 된다. 반드시 서버로 열 것.
`serve.ps1`을 고칠 일이 있으면 **UTF-8 BOM을 유지**해야 한다 — Windows
PowerShell 5.1은 BOM 없는 파일을 ANSI로 읽어서 한글 주석이 깨지고 구문 오류가 난다.

---

## ✅ 해결된 것 — 관리자 기능 2개

**(1) 정보 리셋** — 완료. 전체 / 매칭만 / 행별 개별 삭제 전부 구현.

**(2) 특정 참가자 제외** — **행별 삭제로 대신했다.** `excluded` 플래그 방식은
규칙에 `update`를 열어야 하는데, 그러면 "제출한 응답은 아무도 못 고친다"는
원칙이 깨진다. 부스 규모에서 문제 참가자는 그냥 지우는 게 더 단순하고 안전하다.

**보안 결정 (2026-08-25, 운영진 판단)** — 삭제 권한을 `request.auth != null`
(로그인만 하면 허용)로 두기로 했다. **이메일 allowlist도, 가입 차단도 안 한다.**

배경을 남겨둔다 — 이건 몰라서 빠뜨린 게 아니라 알고 내린 결정이다:

- 2026-08-25 확인 결과 이 프로젝트는 **가입이 열려 있다.** (`accounts:signUp`에
  짧은 비밀번호를 보냈더니 `ADMIN_ONLY_OPERATION`이 아니라 `WEAK_PASSWORD`가
  돌아왔다 — 가입이 막혀 있으면 비밀번호를 보기 전에 차단된다.)
- `apiKey`는 공개 저장소의 `js/config.js`에 있다.
- 따라서 **원리상 누구나 계정을 만들어 로그인한 뒤 participants / matches를
  전부 지울 수 있다.**
- 하루짜리 교내 부스라 실제로 그럴 사람이 없다고 보고 감수하기로 했다.

막고 싶어지면 둘 중 하나만 하면 된다 (코드 변경 없이 콘솔 작업만):

1. Authentication → 설정 → 사용자 작업 → "만들기(가입) 사용 설정" 끄기
2. 또는 `firestore.rules`의 `isStaff()` 안 주석을 풀어 운영진 이메일 목록을
   넣고 재배포

**실질적 완화책**: 행사가 끝나면 곧바로 `participants` / `matches`를 지우거나
Firebase 프로젝트 자체를 삭제할 것. 데이터가 없으면 지워질 것도 없다.

---

## 알려진 한계 (설계상 감수한 것)

`participants` 읽기 권한이 열려 있다. 브라우저에서 매칭을 계산하려면 다른
참가자 문서를 읽어야 하기 때문이다. **프로젝트 ID를 아는 사람은 참가자
인스타 ID를 긁어갈 수 있다.** 무료(Spark) 요금제 + 클라이언트 SDK만으로는
막을 수 없다 (Firestore 규칙은 문서 단위라 특정 필드만 숨기는 게 안 된다).

막으려면 매칭을 Cloud Functions로 옮기고 `allow read`를 `false`로 바꿔야
하며 Blaze(종량제) 요금제가 필요하다.

**시작 화면 문구가 "인스타 ID는 매칭된 상대에게만 공개돼요"인데, 엄밀히는
정확하지 않다.** 문구를 고치든 Functions로 옮기든 결정이 필요하다.

---

## 다음 세션 프롬프트

아래를 그대로 붙여넣으면 이어서 작업할 수 있다.

```
동아리 축제 소개팅 매칭 부스 작업 이어서 할게.

저장소: https://github.com/seopchang/dongari-festival-match
배포: https://seopchang.github.io/dongari-festival-match/
Firebase 프로젝트 ID: duthegee-eaf1b

PROGRESS.md 먼저 읽고 현재 상태 파악해줘.

PROGRESS.md의 "당장 해야 할 것"부터 확인해줘.

로컬은 serve.ps1로 띄우면 된다 (Node/Python 없음).
```

### 배포 상태 (세 곳이 따로 논다 — 헷갈리지 말 것)

| | 무엇 | 어떻게 갱신 |
| --- | --- | --- |
| 내 PC | 원본 | 여기서 고친다 |
| Netlify | 실서비스 | `dist` 폴더를 Deploys 탭에 드래그 |
| GitHub | 백업·이력 | 웹에서 Add file → Upload files |
| Firebase | 규칙·데이터 | 콘솔에서 `firestore.rules` 복붙 → 게시 |

`dist` 는 배포용 사본이라 **저장소에는 안 올린다**(`.gitignore`에 있음).
코드를 고치면 `dist` 를 다시 만들어야 Netlify에 반영된다.

### 남은 일 요약

- [ ] 규칙 재배포 (위 0번) ← **최우선**. 관리자에서 삭제가 되면 배포된 것
- [ ] `ZZ_RULE_TEST_DELETE_ME` 삭제
- [ ] 브라우저 실제 테스트 (위 2번 시나리오)
- [ ] 시작 화면 문구 결정 (아래 "알려진 한계")
- [x] Netlify 배포 — https://sprightly-moxie-6a7c45.netlify.app (claim 완료, 비밀번호 해제됨)
- [ ] Netlify 사이트 이름을 짧은 걸로 변경 → **그 뒤에** QR 제작
- [ ] 주소 확정 후 포스터 QR 만들기 · 인쇄 (SETUP.md 7번)
- [ ] **행사 종료 직후 데이터 삭제** (개인정보 + 위 보안 결정의 완화책)

---

## 참고 문서

- [README.md](README.md) — 구조, 데이터 모델, 매칭 알고리즘, 한계
- [SETUP.md](SETUP.md) — Firebase 설치 절차 (이미 완료했지만 재설치 시 참고)
- [firestore.rules](firestore.rules) — 보안 규칙
- [docs/DESIGN_HANDOFF.md](docs/DESIGN_HANDOFF.md) — 원본 디자인 스펙
