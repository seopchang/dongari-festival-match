# 작업 트리와 저장소 모두 LF로 고정한다.
# Windows 기본값(core.autocrlf=true)이면 개발 서버가 자동 생성 파일을
# CRLF로 다시 써서 내용 변화 없는 diff가 계속 생긴다.
* text=auto eol=lf

# 바이너리로 취급할 것
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
